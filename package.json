{
  "name": "plan95",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently --names \"server,client\" --prefix-colors \"blue,magenta\" \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
